Asset placeholder folder.

This project currently draws the illustration, game sprites, effects, and audio directly with HTML, CSS, Canvas, and the Web Audio API so it can run offline without external files.
